<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- allowBackup="false": tasks.json lives in getFilesDir(), which Auto
         Backup sweeps up wholesale, so leaving it on shipped the user's entire
         task list to their Google Drive quota as a side effect of storing it
         privately. On Android 12+ this flag covers cloud backup only —
         device-to-device transfer is decided in data_extraction_rules.xml,
         where it is deliberately left on so a new phone still inherits the
         list. fullBackupContent is not set: with backups off it is dead
         config on API 30 and below, and ignored on 31+. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- "Mark done" on a reminder, handled without opening the app. The
             patched local-notifications plugin sends action buttons here as a
             package-scoped broadcast instead of launching MainActivity.

             exported="false" plus the setPackage() on the sending side keeps it
             unreachable from other apps. No android:process: TaskStore's lock
             only orders writers within one process. -->
        <receiver
            android:name=".TaskActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.sanjkv.todo.NOTIF_ACTION" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Local notifications.

         POST_NOTIFICATIONS is the Android 13+ runtime prompt; the plugin's
         requestPermissions() triggers it.

         USE_EXACT_ALARM is granted at install with no user toggle, which is
         what keeps a reminder landing on the exact minute rather than being
         batched by Doze. Note the Play Store restricts it to apps whose core
         function is alarms or calendars — fine for sideloading and personal
         builds, but a listing may be challenged at review. SCHEDULE_EXACT_ALARM
         is the fallback for Android 12 (API 31/32), which predates it.

         RECEIVE_BOOT_COMPLETED lets the plugin re-arm pending reminders after
         a reboot; without it everything scheduled is lost on restart. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
